name: Pull Request CI

on: [push, pull_request]

jobs:
    build:
        run-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2
            - name: make
              run: make

